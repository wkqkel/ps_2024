#include <string>
#include <vector>
#include <set>
#include <iostream>
using namespace std;

int solution(vector<int> arr) {
    int n = arr.size();
    int cnt = 0;
    
//     for(int i = 0; i < n; i++){
//         set<int> s1;
//         set<int> s2;
//         s1.remove()
//         if(s1.size() == s2.size()) cnt++;
//     }
    set<int> s1;
    s1.insert(3);
    s1.get(3);
    return cnt;
}